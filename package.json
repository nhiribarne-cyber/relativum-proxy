{
  "name": "relativum-proxy",
  "version": "1.0.0",
  "description": "CORS proxy for Relativum — forwards Yahoo Finance requests",
  "main": "proxy.js",
  "scripts": {
    "start": "node proxy.js"
  },
  "engines": {
    "node": ">=18"
  }
}
